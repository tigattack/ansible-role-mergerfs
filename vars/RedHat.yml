---
distro: "{{ 'fc' if ansible_distribution == 'Fedora' else 'el' }}"
version: "{{ ansible_distribution_major_version }}"
arch: "{{ ansible_userspace_architecture }}"
package_base_url: "https://github.com/trapexit/mergerfs/releases/download/{{ mergerfs_version }}"
package_filename: "mergerfs-{{ mergerfs_version }}-1.{{ distro }}{{ version }}.{{ arch }}.rpm"
package_url: "{{ package_base_url }}/{{ package_filename }}"
