---
mergerfs_prerequisites:
  - fuse
mergerfs_dist: "{{ 'fc' if ansible_facts.distribution == 'Fedora' else 'el' }}{{ ansible_facts.distribution_major_version }}"
mergerfs_arch: "{{ ansible_facts.architecture }}"
mergerfs_pkg_prefix: "mergerfs-"
mergerfs_pkg_suffix: "-1.{{ mergerfs_dist }}.{{ mergerfs_arch }}.rpm"
