---
# Version to install: "latest" version or a specific version number (e.g. "2.28.2")
mergerfs_version: latest

# Mounts to create. For example:
# mergerfs_mounts:
#   - path: /mnt/data
#     branches:
#       - /mnt/data1
#       - /mnt/data2
#     options: allow_other,use_ino
mergerfs_mounts: []
